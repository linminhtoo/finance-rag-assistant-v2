# Copy to `.env` and fill in secrets/paths.
#
# Notes:
# - `.env` is gitignored.
# - Most scripts will still run without `.env` when the relevant variables are not needed.

# --- LLM providers ---
LLM_PROVIDER=openai
OPENAI_API_KEY=

# used during process_html_to_markdown.py
OPENAI_BASE_URL="http://localhost:8993/v1"

# used during build_index.py (via build_index.sh) and main.py
OPENAI_EMBED_BASE_URL="http://localhost:8913/v1"
OPENAI_CHAT_BASE_URL="http://localhost:8993/v1"
# used only by build_index.py (via build_index.sh)
OPENAI_CONTEXT_BASE_URL="http://localhost:8993/v1"

# Optional model overrides (useful for OpenAI-compatible servers like vLLM)
# OPENAI_CHAT_MODEL=
# OPENAI_EMBED_MODEL=
# Generic fallbacks (used for non-OpenAI providers too)
# CHAT_MODEL=
# EMBED_MODEL=
# MISTRAL_API_KEY=
# MISTRAL_CHAT_MODEL=
# MISTRAL_EMBED_MODEL=
# FASTEMBED_EMBED_MODEL=

# --- Tracing (optional) ---
LANGSMITH_TRACING=true
# required when LANGSMITH_TRACING=true
LANGSMITH_API_KEY=

# --- Marker section header base URL override (optional) ---
# used during process_html_to_markdown.py
# use this only if you want `LLMSectionHeaderProcessor` to call a different
# OpenAI-compatible endpoint than the main Marker OCR calls.
# for this, we generally use Qwen/Qwen3-VL-32B-Instruct-FP8
SECTIONHEADER_OPENAI_BASE_URL=http://192.168.3.184:8993/v1

# --- Storage / retrieval ---
RETRIEVER_BACKEND=milvus
# set these paths as needed
# QDRANT_STORAGE_PATH=./data/database/qdrant
# MILVUS_PATH=./data/database/milvus.db
# only set this if you actually have a running Milvus server
# MILVUS_URI=http://localhost:19530
# used by both milvus and qdrant backends for BM25 sparse embeddings
BM25_PATH=./data/database/bm25.pkl
MILVUS_USE_SPARSE=true
MILVUS_COLLECTION_NAME=with_llm_context_27_12_25
# llm or bge-m3
MILVUS_DENSE_EMBEDDING=llm
# bm25 or bge-m3
MILVUS_SPARSE_EMBEDDING=bm25
CONTEXT_STRATEGY="neighbors"
CONTEXT_WINDOW=8

# --- vLLM scripts (optional) ---
VLLM_API_KEY=test
CUDA_VISIBLE_DEVICES=0,1

# --- OpenTelemetry (used by serve_vllm.sh) ---
OTEL_SERVICE_NAME=vllm-server
OTEL_EXPORTER_OTLP_TRACES_INSECURE=true
OTEL_EXPORTER_OTLP_TRACES_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://127.0.0.1:4318/v1/traces
